#include <stdio.h>
#include "lists.h"

void print_list(const list_t *h)
{
	const list_t *current = h;
	while (current != NULL)
	{
		printf("[%u] %s\n", current->len, current->str);
		current = current->next;
	}
}

